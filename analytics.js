/**
 * Vercel Web Analytics — Presy (HTML static / Capacitor)
 * Per Next.js usare: import { Analytics } from '@vercel/analytics/next'
 */
import { inject } from 'https://esm.sh/@vercel/analytics@2.0.1';

inject();
